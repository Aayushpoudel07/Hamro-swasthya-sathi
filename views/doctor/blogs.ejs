<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-body">
            <h2 class="text-center mb-4">Your Blogs</h2>
            <a href="/doctor/create-blog" class="btn btn-success mb-3">Create New Blog</a>

            <table class="table table-bordered" id="example">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Title</th>
                        <th>Content</th>
                        <th>Tags</th>
                        <th>Published At</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% blogs.forEach((blog, index)=> { %>
                        <tr>
                            <td>
                                <%= index + 1 %>
                            </td>
                            <td>
                                <%= blog.title %>
                            </td>
                            <td>
                                <%= blog.content.slice(0, 50) + '...' %>
                            </td>
                            <td>
                                <%= blog.tags %>
                            </td>
                            <td>
                                <%= blog.status %>
                            </td>
                            <td>
                                <a href="/blog/delete/<%= blog.id %>" class="btn btn-danger btn-sm">Delete</a>
                            </td>
                        </tr>
                        <% }); %>
                </tbody>
            </table>
        </div>
    </div>


</div>